# Fix Log - 2026-07-10

## Summary

本次修复围绕任务执行稳定性、任务进度实时显示、测试兼容性与 sandbox 启动问题展开。

## Commits

- Root: `deb26d4` - `docs: mark known issues resolved`
- API: `808ca13` - `fix: harden agent task progress and parsing`
- UI: `7136ddc` - `fix: update task progress panel in real time`
- Sandbox: `ed45191` - `fix: repair sandbox startup and timeout status`

## Changes

### API

- 为工具调用增加超时保护，避免搜索等工具长时间停留在调用中状态。
- 缩短 Bing 搜索 HTTP 超时时间。
- 修复 `RedisStreamTask.destroy()` 遍历任务注册表时被取消任务修改字典导致的 shutdown 异常。
- 修复 ReAct 步骤结果解析：当模型输出 `[EARLY_COMPLETE]` 与 JSON 混合结构时，从解析结果中提取有效步骤结果，避免 Pydantic 将 list 当作 `Step` 校验失败。
- 调整 Planner 更新计划逻辑：重规划时按步骤 id 更新已有步骤并追加新步骤，不再删除已经展示过的步骤，避免任务进度条缩水。
- 补齐 memory/vector 相关兼容实现与测试认证 fixture，恢复后端测试通过。

### UI

- 历史事件归一化兼容 `{ event, data }` 与 `{ type, plan/step/message }` 两种结构。
- 任务进度面板按当前对话轮次合并 `plan` 与 `step` 事件，避免结束后只剩 1 条。
- 折叠态优先显示当前 running/pending 步骤。
- running 步骤计入当前进度，并显示加载图标。
- 缩短聊天 SSE 前端超时，避免连接长时间悬挂。
- 修复若干 hydration/lint 问题：固定 sidebar skeleton 宽度、移除 session item mounted 分支、调整设置页文案。

### Sandbox

- 调整 Dockerfile，移除不可用/不稳定的 PPA 与安装源，改用 Ubuntu 默认 Python、apt Node/npm 与 Playwright Chromium。
- 修复 `cancel_timeout()` 返回字段名 `activate` -> `active`。

### Docs

- 更新 `docs/known-issues.md`，将任务进度实时性与进度条数异常标记为已修复。

## Verification

- API: `51 passed, 20 warnings`
- UI lint: `0 errors, 21 warnings`
- UI build: passed
- Sandbox: Python compile check passed
- Local services checked earlier: UI/API/Sandbox docs endpoints returned `200`

## Follow-up Convention

以后每次代码修复或功能改动，都同步新增或更新一份 Markdown 变更记录，并随代码一起提交。
